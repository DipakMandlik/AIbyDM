{
  "id": "ai-from-scratch/19-capstone-projects/75-end-to-end-eval-runner",
  "slug": "75-end-to-end-eval-runner",
  "phaseSlug": "19-capstone-projects",
  "number": "75",
  "title": "End-to-End Eval Runner",
  "summary": "Five lessons of plumbing, one lesson to glue them. The runner reads the task spec from lesson 70, calls a model through an adapter, scores with lessons 71 and 72, attaches the calibration report from lesson 73, and emits the leaderboard fr...",
  "motto": "Five lessons of plumbing, one lesson to glue them. The runner reads the task spec from lesson 70, calls a model through an adapter, scores with lessons 71 and 72, attaches the calibration report from lesson 73, and emits the leaderboard from lesson 74. Demo self-terminates.",
  "type": "Build",
  "languages": [
    "Python"
  ],
  "prerequisites": [
    "Phase 19 Track B foundations",
    "lessons 70 through 74"
  ],
  "duration": "~90 min",
  "status": "Complete",
  "objectives": [
    "Define a ModelAdapter interface that any model (mock, local, API) can satisfy with a small method surface.",
    "Run the eval over a fixture JSONL file with parallel task execution across a worker pool.",
    "Compose the metric layer (exact_match, F1, BLEU-4, ROUGE-L, code_exec) with the calibration layer in one pass.",
    "Emit per-model EvalRun records and feed them straight into the leaderboard aggregator.",
    "Output both a JSON report and a markdown table; self-terminate with exit zero on a clean run, non-zero on validation or runtime failure."
  ],
  "exercises": [],
  "topics": [
    "Capstone Projects",
    "Build",
    "Python",
    "End",
    "Eval",
    "Runner"
  ],
  "sections": [
    {
      "key": "overview",
      "title": "Overview",
      "markdown": "> Five lessons of plumbing, one lesson to glue them. The runner reads the task spec from lesson 70, calls a model through an adapter, scores with lessons 71 and 72, attaches the calibration report from lesson 73, and emits the leaderboard from lesson 74. Demo self-terminates."
    },
    {
      "key": "objectives",
      "title": "Learning objectives",
      "markdown": "- Define a `ModelAdapter` interface that any model (mock, local, API) can satisfy with a small method surface.\n- Run the eval over a fixture JSONL file with parallel task execution across a worker pool.\n- Compose the metric layer (exact_match, F1, BLEU-4, ROUGE-L, code_exec) with the calibration layer in one pass.\n- Emit per-model `EvalRun` records and feed them straight into the leaderboard aggregator.\n- Output both a JSON report and a markdown table; self-terminate with exit zero on a clean run, non-zero on validation or runtime failure."
    },
    {
      "key": "the-pipeline",
      "title": "The pipeline",
      "markdown": "```mermaid\nflowchart TD\n    A[tasks.jsonl from lesson 70] --> B[validate]\n    B --> C[render prompts]\n    C --> D[model adapter generate]\n    D --> E[post_process]\n    E --> F{metric_name}\n    F -->|exact_match/f1/bleu_4/rouge_l/accuracy| G[score from lesson 71]\n    F -->|code_exec| H[run_candidate from lesson 72]\n    G --> R[EvalRun record]\n    H --> R\n    D --> S[confidence and per-token nll]\n    S --> T[CalibrationReport from lesson 73]\n    R --> U[aggregate from lesson 74]\n    T --> V[per-model calibration block]\n    U --> W[leaderboard JSON + markdown]\n    V --> W\n```\n\nThe runner is the integration point. Each lesson 70 through 74 owns one module that the runner composes. The runner does not duplicate any logic from those modules: it imports them."
    },
    {
      "key": "the-adapter-interface",
      "title": "The adapter interface",
      "markdown": "The adapter is the seam between the runner and any model. The interface is intentionally small.\n\n```python\nclass ModelAdapter:\n    model_id: str\n\n    def generate(self, prompt: str, task: TaskSpec) -> Generation: ...\n```\n\n`Generation` is a dataclass with:\n\n- `text`: the model's free-form output\n- `confidence`: a float in `[0, 1]` representing the model's self-reported probability for the answer\n- `token_nll`: optional sum of negative log-likelihoods over the generated tokens\n- `token_count`: optional number of generated tokens\n\nMock adapters in the runner provide three flavours: `RuleBasedAdapter` (deterministic, near-perfect), `NoisyAdapter` (overconfident, often wrong), and `BiasedAdapter` (good at one category, terrible at another). The demo runs all three over the lesson 70 fixture."
    },
    {
      "key": "parallel-execution",
      "title": "Parallel execution",
      "markdown": "The runner uses `concurrent.futures.ThreadPoolExecutor` to run tasks in parallel per model. The worker count defaults to the smaller of eight and the task count. Threads are sufficient because the bottleneck for real model calls is network I/O. The code-exec path spawns its own subprocess inside the task and the executor only schedules the wait.\n\nFor deterministic tests, the runner exposes `run_eval(adapters, tasks, parallel=False)` so tests can pin the execution order."
    },
    {
      "key": "the-single-pass-scoring-loop",
      "title": "The single-pass scoring loop",
      "markdown": "For each task:\n\n1. Render the prompt (few-shot prefix plus the prompt body).\n2. Call the adapter and time the call.\n3. Post-process the generation per the task's rule.\n4. Dispatch to the metric layer.\n5. Build an `EvalRun` record with the score and metric metadata.\n6. Append the `(confidence, correct)` pair to the calibration buffer.\n\nThe `correct` signal is `score >= 1.0` for exact_match-style metrics (`exact_match`, `accuracy`, `code_exec`) and `score >= 0.5` for graded metrics. The threshold lives in `_correct_from_score` and the runner does not expose a public override."
    },
    {
      "key": "aggregation",
      "title": "Aggregation",
      "markdown": "After every task has a result, the runner calls `aggregate` and `pairwise_diffs` from lesson 74 and `CalibrationReport.from_predictions` from lesson 73. The output is a single JSON envelope:\n\n```json\n{\n  \"leaderboard\": [...],\n  \"pairwise\": [...],\n  \"calibration\": {\n    \"model_id_a\": {\"ece\": 0.04, \"brier\": 0.10, \"populated_bins\": 8, ...},\n    ...\n  },\n  \"summary\": {\n    \"tasks\": 10,\n    \"models\": 3,\n    \"wall_seconds\": 1.2\n  }\n}\n```\n\nThe runner also writes a markdown table to stdout so the user can paste the result into a PR review."
    },
    {
      "key": "self-terminating-demo",
      "title": "Self-terminating demo",
      "markdown": "The demo runs three mock adapters over the ten fixture tasks from lesson 70. Wall time should sit under ten seconds. The exit code is zero on a clean run.\n\nThe clean-run criteria are:\n\n- Every task validated under lesson 70.\n- Every task scored under lessons 71 and 72.\n- The calibration report aggregated under lesson 73 without errors.\n- The leaderboard ranked the rule-based adapter strictly above the random adapter.\n\nIf any of those break, the runner exits non-zero with a structured error in the JSON envelope."
    },
    {
      "key": "what-this-lesson-does-not-do",
      "title": "What this lesson does not do",
      "markdown": "It does not call a real model. It does not implement an API key flow or rate-limit handling. It does not implement streaming or partial generation; the adapter returns one generation per call. It does not do retries or caching. Those concerns live at the adapter layer; the runner is metric-agnostic and provider-agnostic."
    },
    {
      "key": "how-to-read-the-code",
      "title": "How to read the code",
      "markdown": "`main.py` is the integration. It imports from the other five lesson modules through a small `_load_sibling` helper that resolves them by relative path. The dataclasses `Generation`, `EvalReport`, and `ModelAdapter` are defined locally. The mock adapters are at the bottom of the file.\n\nRead `main.py` top to bottom. Skim the imports, then look at `run_eval`, then `_score_one`, then the adapters. The demo at the end is the entry point.\n\nThe tests in `code/tests/test_runner.py` pin the adapter interface, the single-pass loop, the parallel-vs-sequential equivalence, the calibration buffer, and the JSON envelope shape."
    },
    {
      "key": "going-further",
      "title": "Going further",
      "markdown": "This runner is the floor. A production eval system adds: a results cache keyed by `(task_id, model_id, model_version)`, a cost ledger that tracks dollars and tokens per run, a retry layer that backs off on rate limits, a sampling policy for pass-at-k tasks, and a streaming output format for long suites. Each of those is a single concern that wraps the runner without changing the metric or aggregation layers. That separation is the point of the contract.\n\nAdd an adapter for a real provider after you have the mocks working. Pick one with a free tier, write thirty lines of glue, watch the leaderboard light up. Then add the second provider and let the harness do the work."
    }
  ],
  "quiz": [
    {
      "id": "pre-1",
      "stage": "pre",
      "question": "What is the smallest possible surface for the ModelAdapter interface?",
      "options": [
        "generate(prompt, task) returning text plus optional confidence and per-token nll",
        "complete, embed, classify, summarise",
        "init, login, generate, close",
        "load_weights, generate, save_weights"
      ],
      "correct": 0,
      "explanation": "One method covers any adapter. Everything else is optional decoration. The runner does not need a richer surface."
    },
    {
      "id": "pre-2",
      "stage": "pre",
      "question": "Why does the runner accept a parallel flag rather than always running in parallel?",
      "options": [
        "Parallel execution is unsafe in CPython",
        "Tests need deterministic execution order; the flag lets them switch to sequential",
        "Parallel execution requires GPU",
        "ThreadPoolExecutor is not stdlib"
      ],
      "correct": 1,
      "explanation": "Tests want reproducibility. Production wants throughput. The flag splits those concerns without two code paths."
    },
    {
      "id": "check-3",
      "stage": "check",
      "question": "How does the runner determine the correct flag for the calibration buffer?",
      "options": [
        "It calls the adapter twice and compares outputs",
        "It uses exact_match-style metrics at threshold 1.0 and graded metrics at threshold 0.5 by default",
        "It uses the average bin confidence",
        "It uses Brier decomposition"
      ],
      "correct": 1,
      "explanation": "The threshold is metric-aware: exact_match-style metrics use a near-1.0 cutoff, while graded metrics use 0.5 by default."
    },
    {
      "id": "check-4",
      "stage": "check",
      "question": "Why does the runner build EvalRun records and hand them to the aggregator instead of computing the leaderboard inline?",
      "options": [
        "EvalRun is more memory-efficient",
        "It keeps the runner agnostic to the aggregator and lets the leaderboard layer evolve without touching the runner",
        "The aggregator is faster on numpy arrays than dataclasses",
        "EvalRun is required by JSON Schema"
      ],
      "correct": 1,
      "explanation": "Composition over inlining. The runner produces records; the aggregator owns the leaderboard math. Each lesson owns one concern."
    },
    {
      "id": "check-5",
      "stage": "check",
      "question": "What is the role of the perplexity block in the final JSON envelope?",
      "options": [
        "It is required for the leaderboard ranking",
        "It is the input to the calibration report",
        "It carries the held-out language modelling number per model, independent of task scoring",
        "It is computed from the bootstrap CI"
      ],
      "correct": 2,
      "explanation": "Perplexity sits next to the leaderboard, not inside it. It uses the adapter's per-call token NLL and token counts."
    },
    {
      "id": "post-6",
      "stage": "post",
      "question": "What is the clean-run exit criterion of the self-terminating demo?",
      "options": [
        "Every task validated, every task scored, calibration aggregated, and rule-based adapter strictly above the random adapter on the leaderboard",
        "Every task scored above 0.9",
        "ECE below 0.05 for every model",
        "Bootstrap CI excludes zero for every pair"
      ],
      "correct": 0,
      "explanation": "The demo enforces the contract end to end: every layer ran and the most accurate adapter is ranked accordingly."
    }
  ],
  "resources": [
    {
      "label": "main.py",
      "href": "https://github.com/DipakMandlik/AIByDM/tree/main/phases/19-capstone-projects/75-end-to-end-eval-runner/code/main.py",
      "kind": "Code"
    },
    {
      "label": "tests/test_runner.py",
      "href": "https://github.com/DipakMandlik/AIByDM/tree/main/phases/19-capstone-projects/75-end-to-end-eval-runner/code/tests/test_runner.py",
      "kind": "Code"
    },
    {
      "label": "Source lesson",
      "href": "https://github.com/DipakMandlik/AIByDM/tree/main/phases/19-capstone-projects/75-end-to-end-eval-runner",
      "kind": "Source"
    }
  ],
  "sourcePath": "phases/19-capstone-projects/75-end-to-end-eval-runner"
}
